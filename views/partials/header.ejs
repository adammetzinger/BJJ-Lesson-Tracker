<!DOCTYPE html>
<html>
  <head>
    <title><%= title %></title>
    <link rel='stylesheet' href='/stylesheets/style.css' />
  </head>
  <body>
    <nav>
      <a href="/">Home</a>
        <% if (user) { %>
            <div class="user-info"><%= user.name.split(' ')[0] %>
            <div class="review-user"><img src="<%= user.avatar %>" alt="avatar"></div></div>
            <a href="/logout">LOG OUT</a>
        <% } else { %>
            <a href="/auth/google" class="login">LOG IN&nbsp;<img src="https://i.imgur.com/FHjYyi0.png"></a>
        <% } %>
    </nav>
    <h1><%= title %></h1>